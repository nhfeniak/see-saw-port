// Root build file: plugin versions once, for all modules.
plugins {
    // Since AGP 9, Kotlin support is built into the Android plugin itself.
    id("com.android.application") version "9.2.1" apply false
}
